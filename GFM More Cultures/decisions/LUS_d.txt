political_decisions = {
	#Taken by NET, so we need to scope LUS
	LUS_setup = {
		picture = map_balkans
		potential = {
			NOT = { has_global_flag = LUS_setup }
		}
		allow = {}
		effect = {
			set_global_flag = LUS_setup
			LUS = { primary_culture = sorbian }
			#No change needed for LUZ, it already specifies polish
		}
	}
}
